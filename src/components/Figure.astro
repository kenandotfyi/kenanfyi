---
const { caption, fw } = Astro.props;
---

{
  fw ? (
    <figure class="fullwidth">
      <span class="caption fw">{caption}</span>
      <slot />
    </figure>
  ) : (
    <figure>
      <span class="caption">{caption}</span>
      <slot />
    </figure>
  )
}
